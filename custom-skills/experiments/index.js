/**
 * experiments/index.js
 * ────────────────────
 * Work-in-progress ideas and prototypes.
 *
 * This folder is a sandbox — nothing here is expected to be
 * production-ready. It's a place to:
 *   - Try out new APIs or libraries
 *   - Prototype a skill before moving it to a proper folder
 *   - Keep half-finished ideas without cluttering other folders
 *   - Document failed attempts (knowing what doesn't work is valuable)
 *
 * When an experiment graduates to a real skill:
 *   1. Move the file to the appropriate folder (automation/, scraping/, etc.)
 *   2. Clean up the code and add proper comments
 *   3. Export it from that folder's index.js
 *   4. Register it in the root index.js
 *   5. Delete or archive the experiment file
 *
 * Nothing in this folder is exported — it's all exploratory.
 */

// No exports — this folder is for exploration only.
// Files here are drafts, not finished skills.

module.exports = {};
