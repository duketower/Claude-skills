/**
 * scraping/index.js
 * ─────────────────
 * Central export for all scraping skills.
 *
 * This folder holds helpers for:
 *   - HTML parsing and element extraction
 *   - Playwright browser automation patterns
 *   - Pagination handlers (click-next, scroll-load, offset params)
 *   - Data cleaning and normalization after scraping
 *   - Anti-detection techniques (stealth, delays, user-agents)
 *   - Output formatters (CSV, JSON, Excel)
 *
 * Usage:
 *   const { cleanPhone } = require('./scraping');
 *
 * Related project: /Data Scraping — contains full scraper implementations.
 * Skills here are extracted, reusable pieces from those scrapers.
 */

// --- Exports ---
// Uncomment and add as skills are built:

// const { cleanPhone } = require('./cleanPhone');
// const { scrollToBottom } = require('./scrollToBottom');
// const { paginateWithNextButton } = require('./paginateWithNextButton');
// const { saveToCSV } = require('./saveToCSV');

module.exports = {
  // cleanPhone,
  // scrollToBottom,
  // paginateWithNextButton,
  // saveToCSV,
};
